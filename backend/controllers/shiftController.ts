import { NextFunction, Request, Response } from "express";
import { Shift } from "../models/Shift";

export const createShift = async (req: Request, res: Response, next: NextFunction) => {

}

export const availableShifts = async (req: Request, res: Response, next: NextFunction) => {
    
}

export const bookShift = async (req: Request, res: Response, next: NextFunction) => {
    
}

export const viewBookedShifts = async (req: Request, res: Response, next: NextFunction) => {
    
}

export const cancelBookedShift = async (req: Request, res: Response, next: NextFunction) => {
    
}